class MyWidget extends HTMLElement {
  constructor() {
    super();
  }
  // listen for call triggered when we click on a widget card
  // then retrieve this specific widget info
  // expand to full screen
  // add class to shrink card view
  // templatize single widget data
}

window.customElements.define('my-widget', MyWidget);
